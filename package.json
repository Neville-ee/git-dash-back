{
  "name": "github-dashboard-backend",
  "version": "1.0.0",
  "description": "Collects GitHub push & pull-request activity (via webhooks + API polling) and serves it as JSON for the dashboard frontend.",
  "type": "module",
  "main": "src/server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  }
}
